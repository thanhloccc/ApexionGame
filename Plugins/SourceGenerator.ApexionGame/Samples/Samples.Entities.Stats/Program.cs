namespace Samples.Entities.Stats
{
    /// <summary>
    /// Entry point exists only so this project is an Exe and the compiler runs the generators on it.
    /// Nothing here is meant to be executed.
    /// </summary>
    internal static class Program
    {
        private static void Main() { }
    }
}
